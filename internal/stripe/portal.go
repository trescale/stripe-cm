// Portal sync/import functions are colocated with business.go since they share
// the same Stripe account context. This file exists as a placeholder to match
// the intended directory structure — all portal logic lives in business.go.
package stripe
